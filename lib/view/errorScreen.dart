import 'package:flutter/material.dart';

class errorScreen extends StatelessWidget {
  const errorScreen({Key? key}) : super(key: key);
  static const String routeNames = 'errorScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
